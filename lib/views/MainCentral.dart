import 'package:flutter/material.dart';

class MainCentral extends StatelessWidget {
  const MainCentral({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.black,
    );
  }
}
